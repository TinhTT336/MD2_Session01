import React from "react";

export default function Func_Comp() {
  // day la noi code JS
  const sum = (a, b) => {
    return a + b;
  };
  return <h1>Tổng của 3 và 4 là: {sum(3, 4)}</h1>;
}
// react function components
